# Written by `lodger install`. The lodger service user must be a real member
# of the libvirt group, because polkit reads real group membership.
u lodger - "Lodger VM management" /var/lib/lodger /usr/sbin/nologin
m lodger libvirt
