---
default: minor
---

Add the configuration file /etc/lodger/config.toml (listen, uri, state_dir, public_url, trusted_proxies), the `--config` and `--state-dir` flags, the SQLite database in the state directory with mode 0600, and GET /api/health.
