---
default: minor
---

Add the setup, login, and account pages: the web UI now sends visitors to setup or login, and the account page adds and deletes accounts and changes the own password, which needs the current one and ends every other session.
