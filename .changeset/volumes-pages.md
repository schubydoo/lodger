---
default: minor
---

Show the volumes of a running pool on its page, with the format, the size, the space on disk, and the VMs and qcow2 overlays that use each one. The page creates qcow2 and raw volumes, and it deletes a volume after its name is typed, unless something uses it.
