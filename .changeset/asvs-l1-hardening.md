---
default: patch
---

Send `Strict-Transport-Security` on HTTPS answers, name `charset=utf-8` on every text asset, and clear the cached data in the browser when a session expires, not only on logout.
