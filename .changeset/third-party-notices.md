---
default: patch
---

Ship complete third-party notices: /third-party-notices.txt now lists every Rust crate in the binary and every web package in the UI, generated at build time, and the build fails on a license outside the allowed list.
