---
default: minor
---

Explain known libvirt errors. A failed action with a known error also shows the cause, the fix, and the host commands, for example the AppArmor rule that a NIC hot-plug needs on Debian 13. Other errors show libvirt's text unchanged.
