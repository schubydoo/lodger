---
default: minor
---

Add reboot, pause, resume, autostart, and delete for VMs: delete needs the typed VM name and a shut-off VM, can also delete the VM's volumes that no other VM uses, and lists every kept disk with the reason; after a shutdown request that the guest ignores for 120 seconds, the VM row offers Force off.
