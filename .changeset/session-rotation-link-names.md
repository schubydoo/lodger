---
default: patch
---

A login now ends the session that the browser had before, a password change gives the browser a new session, and links to a VM, pool, or network work when its name holds `#`, `?`, `%`, or a space.
