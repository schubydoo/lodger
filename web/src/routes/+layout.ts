// Lodger's UI is a client-side single-page app, served from files embedded in the
// Rust binary. No page renders on a server, and no page is prerendered.
export const ssr = false;
export const prerender = false;
