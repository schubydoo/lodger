# Allow-list and exclusions for the typos spell checker (see ci.yml).
[files]
extend-exclude = [
    "Cargo.lock",
    "LICENSE",
    "pnpm-lock.yaml",
    "web/pnpm-lock.yaml",
    # Data: the most common passwords, which are not words.
    "crates/lodger-core/src/password/common.txt",
]
