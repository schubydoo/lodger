Lodger Rust crate notices
=========================

The lodger binary links the Rust crates below. Each license is reproduced
once, followed by the crates that use it. cargo-about generated this file
from Cargo.lock; do not edit it by hand.
{{#each licenses}}


-----------------------------------------------------------------------------
{{name}} ({{id}})
Used by:
{{#each used_by}}
  - {{crate.name}} {{crate.version}}{{#if crate.repository}} ({{crate.repository}}){{/if}}
{{/each}}

{{{text}}}
{{/each}}
